#comparing strings in python

s = input("what is s: ")
t = input("What is t: ")

if s == t:
    print("s is the same as t")
else:
    print("s is not the same as t")

#in C, strings cannot be compared this way, as strings are just char*, pointing to different memory addresses
